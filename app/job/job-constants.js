const JOB_RESULT_CALLBACK_TYPE = {
  // DIRECT: 'direct',
  POLLING: 'polling',
  // callback_report: 'callback'
}

const JOB_STATUS_RESULT = {
  RUNNING: 'RUNNING',
  SUCCESS: 'SUCCESS',
  FAILED:'FAILED'
}

const RESPONSE_JOB_REQUEST = {
  JOB_REQUEST_FIELD : 'job_request'
}

export {
  JOB_RESULT_CALLBACK_TYPE,
  JOB_STATUS_RESULT,
  RESPONSE_JOB_REQUEST
}
